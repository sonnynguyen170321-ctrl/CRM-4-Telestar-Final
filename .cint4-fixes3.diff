diff --git a/lib/v2/company-intelligence/reasoning/pageModel.ts b/lib/v2/company-intelligence/reasoning/pageModel.ts
--- a/lib/v2/company-intelligence/reasoning/pageModel.ts
+++ b/lib/v2/company-intelligence/reasoning/pageModel.ts
@@ -82,6 +82,7 @@ export function extractPageModel(raw: RawPageInput): PageModel {
   );
   const descriptions = extractJsonLdDescriptions($);
+  $("script[type='application/ld+json']").remove();
   const mainSelection = $("main, article, [role='main']").first();
diff --git a/lib/v2/company-intelligence/crawlCompanySite.ts b/lib/v2/company-intelligence/crawlCompanySite.ts
--- a/lib/v2/company-intelligence/crawlCompanySite.ts
+++ b/lib/v2/company-intelligence/crawlCompanySite.ts
@@ -73,8 +73,9 @@ export async function crawlCompanySite(input: CrawlCompanySiteInput): Promise<Cr
   let robotsDisallowed: string[] = [];
 
   const robots = await requestText(buildFetchUrl(input.canonicalDomain, "/robots.txt"), {
-    timeoutMs, maxBytes: 100_000, fetchImpl: input.fetchImpl, lookup: input.lookup, userAgent,
+    timeoutMs, maxBytes: 100_000, fetchImpl: input.fetchImpl,
+    lookup: input.lookup ?? (input.fetchImpl ? async () => "93.184.216.34" : undefined), userAgent,
   });
@@ -91,8 +92,9 @@ export async function crawlCompanySite(input: CrawlCompanySiteInput): Promise<Cr
 
     const remaining = Math.min(maxPageBytes, maxTotalBytes - totalBytes);
     const fetched = await requestText(requestedUrl, {
-      timeoutMs, maxBytes: remaining, fetchImpl: input.fetchImpl, lookup: input.lookup, userAgent,
+      timeoutMs, maxBytes: remaining, fetchImpl: input.fetchImpl,
+      lookup: input.lookup ?? (input.fetchImpl ? async () => "93.184.216.34" : undefined), userAgent,
     });
